// for Address of an elements use the ampersand symobl(&).
#include <iostream>
using namespace std;
int main(){
	int a;
	float b;
	long long c;
	cout<<"The address of int is "<<&(a)<<endl<<"The address of float is "<<&(b)<<endl<<"the address of long long  is "<<&(c);
	return 0;
}
